<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.english">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.English">
        <activity android:name="Listen.ListView"></activity>
        <activity android:name="Listen.Listen_main" />
        <activity android:name="VocabularyFruits.Vocabulary_fruits" />
        <activity android:name="QuizList.Quizlv2" />
        <activity android:name="TopicVocabulary.GridTopic" />
        <activity
            android:name="Vocabulary.VResult"
            android:theme="@style/FullScreen" />
        <activity android:name="Vocabulary.Vocabulary_main" />
        <activity android:name="QuizList.QuizListView" />
        <activity android:name=".Result" />
        <activity android:name=".quiz2" />
        <activity android:name=".QuizTextToSpeech" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>